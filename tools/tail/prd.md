请使用Go语言开发一个Windows平台可运行、兼容Linux用法的tail命令行工具，严格实现以下功能：
0. 项目使用go mod 管理。
1. 基础功
- -n：显示文件末尾N行，如 tail -n 20 test.txt
- 默认显示末尾10行
- -f：实时监控文件新增内容（日志实时输出，模拟Linux tail -f）
2. 高级功能
- 支持同时监控多个文件
- 输出带文件名前缀
- 兼容Windows路径、中文文件、中文日志
- 处理文件换行、大文件读取，性能稳定
3. 技术要求
- 仅使用Go标准库，无第三方依赖
- 支持CMD/PowerShell
- 代码结构清晰，注释完整
4. 交付内容
- 完整main.go代码，代码分目录，每个功能模块一个目录
- 编译命令 go build -o tail.exe main.go
- 详细使用示例
5. 需要测试生成的命令, 测试上面的功能都实现了。
6. top 命令需要加到环境变量中，方便直接在CMD/PowerShell中使用。